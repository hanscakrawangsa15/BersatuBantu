# Copy this to .env and fill the value
MIDTRANS_SERVER_KEY=Mid-server-REPLACE_ME
# Optionally restrict CORS origins (comma separated). Leave empty to allow all origins during local dev
CORS_ALLOWED_ORIGINS=http://localhost:51567,https://localhost:51567

# Optional: if you want the server to update your Supabase DB on webhook events
SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_SERVICE_KEY=supabase-service-role-key-REPLACE_ME
# Optional: Admin key to secure the /admin/create-profile endpoint. Set a long random secret
# on your deployed server and pass it in the `x-admin-key` header when requesting the endpoint.
ADMIN_PROFILE_KEY=replace-with-a-long-random-secret